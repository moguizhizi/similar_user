graph_path_limit:
  bands:
    - max_g_count: 49
      per_g: 10
    - max_g_count: 199
      per_g: 6
    - per_g: 4
  max_limit_source: "total_paths"
training_date_split:
  min_training_dates: 5
  before_ratio: 4
  after_ratio: 1
pattern_path_storage:
  output_dir: "data/pattern_paths"
